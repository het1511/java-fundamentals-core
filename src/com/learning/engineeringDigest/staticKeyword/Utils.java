package com.learning.engineeringDigest.staticKeyword;

public class Utils {
    //We will use static for general methods that can be called without creating object
    public static int min(int a, int b) {
        return Math.min(a, b);
    }
}
